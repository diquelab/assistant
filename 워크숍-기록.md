# 워크숍 실습 전체 기록
> Claude Code로 진행한 업무 대시보드 프로젝트 — 재현 가능한 단계별 가이드

---

## 목차
1. [외부 사이트 분석 → 디자인 가이드 제작](#1-외부-사이트-분석--디자인-가이드-제작)
2. [탭 네비게이션 전체 페이지 적용](#2-탭-네비게이션-전체-페이지-적용)
3. [SVG 파일에 네비게이션 추가하는 법](#3-svg-파일에-네비게이션-추가하는-법)
4. [슬래시 커맨드(/김비서) 제작](#4-슬래시-커맨드김비서-제작)
5. [데이터 기반 대시보드 자동 업데이트](#5-데이터-기반-대시보드-자동-업데이트)
6. [디자인 시스템 전체 페이지 일괄 적용](#6-디자인-시스템-전체-페이지-일괄-적용)
7. [GitHub 업로드 (.env 보안 포함)](#7-github-업로드-env-보안-포함)
8. [Vercel 배포 후 버그 수정](#8-vercel-배포-후-버그-수정)

---

## 1. 외부 사이트 분석 → 디자인 가이드 제작

### 무엇을 했나
특정 웹사이트(Creative Education Foundation)의 디자인 시스템을 분석해서 `design-guide.html` 문서로 만들었다.

### 방법
1. 브라우저로 대상 사이트 접속
2. 색상, 타이포그래피, 컴포넌트, 레이아웃 패턴 파악
3. 추출한 내용을 HTML 문서로 정리

### 추출해야 할 항목
| 항목 | 확인 방법 |
|---|---|
| 주색상 (Primary Color) | 버튼, 링크, 강조 요소의 색 |
| 보조색 (Secondary) | 카드 헤더, 날짜 박스 등 |
| 배경색 | body, section 배경 |
| 폰트 패밀리 | 개발자 도구 → Elements → Computed |
| 타이포그래피 계층 | h1/h2/h3 크기·굵기 |
| 버튼 스타일 | border-radius, padding, hover 효과 |
| 카드 스타일 | 테두리, 그림자, 라운드 정도 |
| 그림자 값 | box-shadow 수치 |
| 간격 규칙 | 패딩·마진 스케일 |

### CEF에서 추출한 핵심 값 (이 프로젝트 기준)
```css
/* 색상 */
--primary:    #7BC43F;   /* 녹색 - CTA, 강조, 링크 */
--primary-dark: #6AB030; /* 호버 상태 */
--secondary:  #555555;   /* 카드·버튼 보조 */
--bg-light:   #F5F7FA;   /* 페이지 배경 */
--text-main:  #333333;   /* 본문 */

/* 컴포넌트 */
--radius-btn:     6px;
--radius-card:    8px–12px;
--shadow-light:   0 2px 8px rgba(0,0,0,0.05);
--shadow-standard:0 4px 16px rgba(0,0,0,0.08);
--shadow-btn-hover: 0 4px 12px rgba(123,196,63,0.3);
```

### 디자인 가이드 HTML 구조
```
design-guide.html
├── 색상 팔레트 (color-box 그리드)
├── 타이포그래피 계층 (샘플 텍스트)
├── 컴포넌트 (버튼, 카드, 링크)
├── 레이아웃 패턴
├── 디자인 패턴 (호버, 간격, 그림자)
└── 반응형 규칙
```

---

## 2. 탭 네비게이션 전체 페이지 적용

### 무엇을 했나
여러 HTML 페이지를 오가는 공통 탭 네비게이션 바를 만들고, **모든 페이지에** 동일하게 적용했다.

### 핵심 코드 (헤더 + 탭 구조)
```html
<header>
  <div class="header-content">
    <a href="index.html" class="back-link">← 메인</a>
    <button class="theme-toggle" id="themeToggle" aria-label="테마 전환">
      <div class="toggle-circle">☀️</div>
    </button>
  </div>
  <nav class="tab-nav">
    <div class="tab-nav-inner">
      <a href="dashboard.html"    class="tab-btn active">🏠 대시보드</a>
      <a href="meeting-result.html" class="tab-btn">📋 회의록</a>
      <a href="chart.html"        class="tab-btn">📈 매출 현황</a>
      <a href="diagram.html"      class="tab-btn">🔄 업무 프로세스</a>
      <a href="report.html"       class="tab-btn">🔍 사이트 분석</a>
      <a href="design-guide.html" class="tab-btn">🎨 디자인 가이드</a>
    </div>
  </nav>
</header>
```

> **규칙:** 각 페이지에서 자기 자신의 링크에만 `class="tab-btn active"` 추가

### CSS (헤더를 sticky로 고정)
```css
header {
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(20px);
  position: sticky;
  top: 0;
  z-index: 100;
  border-bottom: 1px solid rgba(255,255,255,0.3);
}
.tab-nav { border-top: 1px solid rgba(0,0,0,0.06); margin-top: 10px; }
.tab-nav-inner {
  display: flex; gap: 2px; overflow-x: auto; scrollbar-width: none;
}
.tab-btn {
  padding: 10px 18px;
  border: none; border-bottom: 3px solid transparent;
  color: #777; font-weight: 600; text-decoration: none;
  border-radius: 8px 8px 0 0;
  transition: all 0.25s ease;
}
.tab-btn:hover  { color: #7bc43f; background: rgba(123,196,63,0.07); }
.tab-btn.active { color: #7bc43f; border-bottom-color: #7bc43f;
                  background: rgba(123,196,63,0.1); }
```

### 다크 모드 지원 (JS)
```js
const themeToggle = document.getElementById('themeToggle');
const savedTheme = localStorage.getItem('theme') || 'light';
if (savedTheme === 'dark') document.body.classList.add('dark-mode');
themeToggle.addEventListener('click', () => {
  document.body.classList.toggle('dark-mode');
  localStorage.setItem('theme',
    document.body.classList.contains('dark-mode') ? 'dark' : 'light');
});
```

---

## 3. SVG 파일에 네비게이션 추가하는 법

### 문제
`diagram.svg`는 XML 파일이라 `<header>`, `<nav>`, `<script>` 같은 HTML 태그를 직접 넣을 수 없다.

### 해결책: HTML 래퍼 페이지 만들기
`diagram.html`을 새로 만들어서 SVG를 **인라인으로 embed** 하고, 그 주변에 HTML 네비게이션을 배치한다.

```html
<!-- diagram.html 구조 -->
<!DOCTYPE html>
<html>
<head>...</head>
<body>
  <header>  <!-- 공통 탭 네비게이션 (active: 업무 프로세스) -->
    ...
  </header>
  <div class="container">
    <div class="svg-wrapper">
      <!-- SVG 내용을 직접 복사해서 붙여넣기 -->
      <svg viewBox="0 0 800 200" ...>
        ...
      </svg>
    </div>
  </div>
</body>
</html>
```

### 이후 처리
다른 모든 페이지에서 `diagram.svg` 링크를 `diagram.html`로 교체:
```bash
# 여러 파일 일괄 교체 (perl 사용)
for f in dashboard.html meeting-result.html chart.html report.html design-guide.html; do
  perl -pi -e 's/diagram\.svg/diagram.html/g' "$f"
done
```

---

## 4. 슬래시 커맨드(/김비서) 제작

### 슬래시 커맨드란
Claude Code에서 `/커맨드명` 을 입력하면 자동 실행되는 자동화 스크립트.  
`.claude/commands/` 폴더에 **마크다운 파일(.md)** 로 저장하면 된다.

### 파일 위치
```
프로젝트폴더/
└── .claude/
    └── commands/
        └── 김비서.md   ← 이 파일이 /김비서 커맨드가 됨
```

### 커맨드 파일 작성법
```markdown
당신은 '김비서'입니다. 아래 3단계를 순서대로 실행하세요.

## 1단계 — 데이터 읽기
- dashboard.html, meeting-result.html, chart.html 파일 읽기
- 김비서-데이터/ 폴더의 모든 파일 읽기

## 2단계 — 대시보드 업데이트
- 완료된 항목 → checkbox checked 처리
- 마감 초과 항목 → 빨간색 스타일 + 배지 추가
- 데이터와 화면 수치 불일치 시 동기화

## 3단계 — 오늘의 브리핑 출력
[원하는 출력 형식 정의]
```

### 포인트
- 파일명이 곧 커맨드명 (한글도 가능)
- 단계가 명확할수록 Claude가 정확히 수행
- 복잡한 로직도 자연어로 기술하면 됨

---

## 5. 데이터 기반 대시보드 자동 업데이트

### 데이터 파일 구조 (이 프로젝트 기준)
```
김비서-데이터/
├── 업무목록.csv       # 할 일 목록 (우선순위, 마감일, 담당자, 상태)
├── 프로젝트현황.csv   # 프로젝트 진행률
├── 주간일정.txt       # 이번 주 일정
└── 회의록.txt         # 최근 회의 결과 및 액션 아이템
```

### 마감 초과 표시 패턴
```css
/* CSS 클래스 정의 */
.todo-meta.overdue    { color: #e53935 !important; }
.todo-meta.due-soon   { color: #f57c00 !important; }
.overdue-badge {
  display: inline-block;
  background: rgba(229,57,53,0.12); color: #e53935;
  padding: 2px 7px; border-radius: 4px;
  font-size: 0.78em; font-weight: 700; margin-left: 6px;
}
.due-soon-badge {
  background: rgba(245,124,0,0.12); color: #f57c00;
  /* 나머지 동일 */
}
.todo-title.done { text-decoration: line-through; opacity: 0.45; }
```

```html
<!-- HTML 적용 예시 -->
<div class="todo-meta overdue">
  <span class="priority-badge priority-high">높음</span>
  담당: 김대리 | 마감: 2026-03-12
  <span class="overdue-badge">⚠ 마감 초과</span>
</div>
```

### 브리핑 출력 형식
```
📅 [오늘 날짜] 업무 브리핑
━━━━━━━━━━━━━━━━━━━━━━━━━━

🔴 즉시 처리 필요 (마감 초과 또는 오늘 마감)
  • [항목명] — 담당: [담당자] (마감: [날짜])

📋 이번 주 할 일
  • [항목명] — 담당: [담당자] (마감: [날짜], 우선순위: 높음/보통/낮음)

📅 오늘 일정
  • [시간] [일정명]

📊 프로젝트 현황 요약
  • [프로젝트명]: [진행률]% — [한 줄 상태]

💬 한마디
  [응원 메시지]
```

---

## 6. 디자인 시스템 전체 페이지 일괄 적용

### 전략: 색상값 일괄 치환
디자인 시스템을 바꿀 때 각 파일을 하나씩 수정하지 않고, `perl`로 모든 파일의 색상을 한 번에 교체한다.

```bash
# 적용 대상 파일 목록
FILES="dashboard.html meeting-result.html chart.html report.html diagram.html index.html"

for f in $FILES; do
  perl -pi -e '
    s/#667eea/#7bc43f/g;          # 주색상 (파랑 → 그린)
    s/#764ba2/#6ab030/g;          # 보조색 (보라 → 다크그린)
    s/rgba\(102, 126, 234,/rgba(123, 196, 63,/g;  # 반투명 주색상
    s/rgba\(31, 38, 135,/rgba(0, 0, 0,/g;         # 그림자 틴트 → 중성
    s/#f0f4ff/#f5f7fa/g;          # 배경 틴트 (파란빛 → 중성)
    s/#b0b0ff/#9dda6f/g;          # 다크모드 강조 텍스트
  ' "$f"
done
```

### 주의사항

**1. border 단축어 vs border-left 충돌**
```css
/* ❌ 잘못된 예 — border-left가 border에 덮어씌워짐 */
body.dark-mode .card {
  border: 1px solid rgba(255,255,255,0.1);  /* 이게 border-left까지 덮음 */
}

/* ✅ 올바른 예 — dark mode에도 border-left 명시 */
body.dark-mode .card {
  border: 1px solid rgba(255,255,255,0.1);
  border-left: 4px solid rgba(123,196,63,0.7);  /* 반드시 다시 선언 */
}
```

**2. 흰 배경 위의 투명 카드**
```css
/* ❌ 흰 배경에서 안 보임 */
.card {
  background: rgba(255,255,255,0.7);
  border: 1px solid rgba(255,255,255,0.5);  /* 완전히 투명 */
}

/* ✅ 올바른 예 */
.card {
  background: #ffffff;
  border: 1px solid rgba(0,0,0,0.08);  /* 눈에 보이는 경계선 */
}
```

**3. 차트 JS 색상은 별도 처리**
Canvas로 그린 차트는 CSS가 아닌 JavaScript 안에 색상이 있다.
```bash
# 차트 색상도 별도로 교체
perl -pi -e '
  s/#1e90ff/#7bc43f/g;
  s/#4169e1/#6ab030/g;
  s/#20b2aa/#555555/g;
' chart.html
```

---

## 7. GitHub 업로드 (.env 보안 포함)

### .gitignore 필수 항목
```
.env.local               # API 토큰, 시크릿 키
.env                     # 환경변수
.DS_Store                # macOS 메타데이터
.claude/settings.local.json  # Claude Code 로컬 설정
```

### 민감한 파일 커밋 전 확인
```bash
# 스테이징 후 status로 반드시 확인
git add .
git status   # .env.local이 목록에 없어야 함
```

### 토큰을 이용한 GitHub 인증 방법
```bash
# .env.local에서 토큰을 읽어 remote URL에 삽입
TOKEN=$(grep GITHUB_TOKEN .env.local | cut -d= -f2)
git remote add origin "https://${TOKEN}@github.com/유저명/레포명.git"
```
> 이 방법은 토큰을 터미널에 직접 입력하지 않아 안전하다.

### 전체 흐름
```bash
# 1. .gitignore 생성
# 2. git 초기화
git init
git branch -M main

# 3. remote 추가 (토큰 인증)
TOKEN=$(grep GITHUB_TOKEN .env.local | cut -d= -f2)
git remote add origin "https://${TOKEN}@github.com/유저명/레포명.git"

# 4. 스테이징 & 커밋
git add .
git status   # 민감 파일 제외 확인
git commit -m "커밋 메시지"

# 5. 푸시
git push -u origin main
```

### 이후 변경사항 반영
```bash
git add 변경된파일.html
git commit -m "fix: 문제 설명"
git push
```
Vercel은 GitHub main 브랜치 push를 감지하여 자동 배포한다.

---

## 8. Vercel 배포 후 버그 수정

### 자주 발생하는 문제들

#### 문제 1: 카드가 투명하게 보임
**원인:** 흰 배경 위에 반투명 흰색 배경 + 반투명 흰색 테두리  
**증상:** 카드 구분이 안 되고 텍스트만 떠있는 것처럼 보임

```css
/* ❌ 문제 코드 */
.card {
  background: rgba(255,255,255,0.7);   /* 흰 배경에서 안 보임 */
  border: 1px solid rgba(255,255,255,0.5);  /* 완전 투명 */
}

/* ✅ 수정 코드 */
.card {
  background: #ffffff;
  border: 1px solid rgba(0,0,0,0.08);
}
```

#### 문제 2: backdrop-filter가 작동 안 함
**원인:** 일부 브라우저(특히 모바일)에서 `backdrop-filter: blur()` 미지원  
**해결:** `background`를 완전 불투명으로 대체하거나 fallback 추가

#### 문제 3: 다크모드에서 border-left 사라짐
**원인:** 다크모드 `border` 단축어가 `border-left` 덮어씀  
**해결:** 다크모드 규칙에 `border-left` 재선언

#### 문제 4: emoji/아이콘이 일부 기기에서 안 보임
**원인:** OS별 emoji 렌더링 차이  
**해결:** 중요한 아이콘은 이미지 또는 SVG 아이콘으로 대체

---

## 전체 프로젝트 파일 구조

```
워크숍-실습/
├── index.html              # 랜딩 페이지
├── dashboard.html          # 메인 대시보드
├── meeting-result.html     # 회의록
├── chart.html              # 매출 현황 차트
├── diagram.html            # 업무 프로세스 (SVG 래퍼)
├── diagram.svg             # 원본 SVG
├── report.html             # 사이트 분석 보고서
├── design-guide.html       # 디자인 가이드
│
├── 김비서-데이터/
│   ├── 업무목록.csv
│   ├── 프로젝트현황.csv
│   ├── 주간일정.txt
│   └── 회의록.txt
│
├── .claude/
│   └── commands/
│       └── 김비서.md       # /김비서 슬래시 커맨드
│
├── .env.local              # GitHub 토큰 (gitignore됨)
├── .gitignore
└── 워크숍-기록.md          # 이 문서
```

---

## 핵심 교훈 요약

| 상황 | 해결책 |
|---|---|
| 여러 파일 색상 일괄 변경 | `perl -pi -e 's/old/new/g' *.html` |
| SVG에 네비게이션 추가 | HTML 래퍼 페이지 만들어 인라인 embed |
| 자주 쓰는 작업 자동화 | `.claude/commands/` 슬래시 커맨드 |
| 흰 배경 카드 안 보임 | `background: #fff` + `border: 1px solid rgba(0,0,0,0.08)` |
| dark mode border 깨짐 | dark mode 규칙에 `border-left` 재선언 |
| 토큰 안전하게 git push | `.env.local`에서 읽어 remote URL에 삽입 |
| 배포 후 확인 | Vercel 자동 배포 → GitHub push로 트리거 |
